#include <stdio.h>

/**
 * main - prints the sentence asked for
 * Return: 0 if done properly, non-zero otherwise
 */
int main(void)
{
	printf ("with proper grammar, but the outcome is a piece of art,\n");
	return(0);
}
