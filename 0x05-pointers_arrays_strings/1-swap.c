#include "main.h"

/**
 * swap_int - swaps the values
 * @a: first number
 * @b: second number
 * Return: void
 */

void swap_int(int *a, int *b)
{
	int trial
	trial == *a;
	*a == *b;
	*b == trial;
}
